# TEB local planner parameters — forward-preferred mecanum profile.
# Keep a small lateral trim for obstacle avoidance while avoiding the visually
# unstable diagonal/rotate motion produced by a fully free omni profile.
TebLocalPlannerROS:

  odom_topic: /odom
  map_frame: map

  # 30 cm x 20 cm rectangular chassis.
  footprint_model:
    type: "polygon"
    vertices: [[-0.15, -0.10], [-0.15, 0.10], [0.15, 0.10], [0.15, -0.10]]

  min_turning_radius: 0.0
  wheelbase: 0.25
  cmd_angle_instead_rotvel: false

  # Velocity limits: normal travel is forward; lateral motion is corrective.
  max_vel_x: 0.22
  max_vel_x_backwards: 0.08
  max_vel_y: 0.04
  max_vel_trans: 0.22
  min_vel_trans: 0.01
  max_vel_theta: 0.60
  min_vel_theta: 0.08

  # --- 加速度限制 ---
  acc_lim_x: 0.35
  acc_lim_y: 0.15
  acc_lim_theta: 0.80

  # --- 轨迹优化 ---
  dt_ref: 0.30
  dt_hysteresis: 0.08
  teb_autosize: true
  global_plan_overwrite_orientation: true
  allow_init_with_backwards_motion: false
  max_global_plan_lookahead_dist: 2.2
  global_plan_viapoint_sep: 0.35
  max_samples: 500
  no_inner_iterations: 4
  no_outer_iterations: 3
  optimization_activate: true
  optimization_verbose: false

  # --- 目标容差：位置准 + 朝向准 ---
  xy_goal_tolerance: 0.15
  yaw_goal_tolerance: 0.15
  free_goal_vel: false

  # --- 障碍物 ---
  min_obstacle_dist: 0.12
  inflation_dist: 0.22
  include_costmap_obstacles: true
  costmap_obstacles_behind_robot_dist: 1.2
  obstacle_poses_affected: 30

  # --- 可行性检查 ---
  feasibility_check_no_poses: 2

  # --- 权重：偏好前向，允许微调横移 ---
  weight_kinematics_nh: 0.0
  weight_kinematics_forward_drive: 20.0
  weight_max_vel_y: 1.0
  weight_acc_lim_y: 0.5
  weight_kinematics_turning_radius: 0.0
  weight_optimaltime: 1.5
  weight_shortest_path: 10.0
  weight_obstacle: 60.0
  weight_inflation: 0.6
  weight_dynamic_obstacle: 10.0
  weight_velocity_obstacle: 10.0
  weight_acceleration_obstacle: 10.0
  weight_parallelity: 5.0
  weight_custom_obstacle: 10.0
  weight_viapoint: 12.0

  # --- 其它 ---
  enable_homotopy_class_planning: false
  enable_multiple_start_poses: false
  exact_arc_length: false
  shrink_horizon_backup: true
  shrink_horizon_min_duration: 8.0
  penalty_epsilon: 0.05

  # --- 振荡恢复 ---
  oscillation_recovery: true
  oscillation_v_eps: 0.12
  oscillation_omega_eps: 0.12
  oscillation_recovery_min_duration: 6.0
  oscillation_filter_duration: 4.0
